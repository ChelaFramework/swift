# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'Chela' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Chela
  pod 'Alamofire', '~> 4.8.0'

  target 'ChelaTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
